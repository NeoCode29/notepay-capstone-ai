# Render deployment — AI Flask API (CPU-only server)
# Sama dengan venv-tf lokal tapi pakai tensorflow-cpu agar lebih ringan

# TF + Keras — pin ke versi SAMA dengan training lokal agar .keras bisa di-load
# (model disimpan dengan keras==3.12.2, harus konsisten)
tensorflow-cpu>=2.16.0
keras==3.12.2

# YOLO (termasuk torch-cpu secara otomatis)
ultralytics==8.4.50

# Flask server
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# HF Hub — download model saat startup
huggingface_hub>=0.25.0

# Image processing
opencv-python-headless>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
